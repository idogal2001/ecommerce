{
	"name": "ecommerce-server",
	"version": "0.0.1",
	"private": true,
	"workspaces": [
		"apps/*"
	],
	"scripts": {
		"build": "turbo run build",
		"start": "set NODE_ENV=development&& sh validate_env.sh&& turbo run start --no-cache --parallel --continue --no-daemon",
		"dev": "set NODE_ENV=development&& turbo run dev --no-cache --parallel --continue --no-daemon --concurrency 20",
		"dev:debug": "set NODE_ENV=development&& sh validate_env.sh&& turbo run dev:debug --no-cache --parallel --continue --no-daemon"
	},
	"devDependencies": {
		"turbo": "^1.11.1"
	},
	"packageManager": "npm@8.19.2",
	"dependencies": {
		"dotenv": "^17.0.0"
	}
}
